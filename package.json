{
  "name": "dynamico",
  "private": true,
  "version": "0.0.0",
  "description": "Runtime React renderer that loads components from a directory and updates them live, on web and Expo.",
  "packageManager": "pnpm@10.30.3",
  "scripts": {
    "build": "pnpm -r --filter \"./packages/*\" build",
    "dev:registry": "pnpm --filter @omriashke/dynamico-registry dev",
    "dev:cli": "pnpm --filter @omriashke/dynamico-cli dev",
    "dev:web": "pnpm --filter web-host dev",
    "dev:book": "pnpm --filter @omriashke/dynamico-book build",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
